# Bug Reports (Task2 API tests)

Дата прогона: 2026-03-25  
Команда: `mvn test -q`  
Итог: `77` тестов, `54` passed, `23` failed.

## BUG-01: Некорректный контракт успешного ответа `POST /api/1/item`
- Кейсы: `TC-01`, `TC-02`, `TC-03`, `TC-04`, `TC-63`
- Ожидалось: JSON с полями `id`, `sellerId`, `name`, `price`, `statistics`, `createdAt`.
- Фактически: возвращается только поле `status` со строкой вида `"Сохранили объявление - <uuid>"`.
- Влияние: невозможно проверить контракт и маппинг `sellerID -> sellerId` по спецификации.

## BUG-02: Не валидируется `name` из пробелов
- Кейс: `TC-27`
- Ожидалось: `400`.
- Фактически: `200`.

## BUG-03: Неверная обработка граничных значений `price` и `statistics.contacts`
- Кейсы: `TC-28`, `TC-33`
- Ожидалось: `200` (`price=0`, `statistics.contacts=0`).
- Фактически: `400` с ошибкой обязательности поля.

## BUG-04: Не валидируются отрицательные значения при создании объявления
- Кейсы: `TC-29`, `TC-34`, `TC-35A`, `TC-35B`, `TC-36`
- Ожидалось: `400`.
- Фактически: `200`.

## BUG-05: Не валидируется сверхдлинный `name` при создании объявления
- Кейс: `TC-38`
- Ожидалось: `400`.
- Фактически: `200`.

## BUG-06: Не валидируются лишние поля при создании объявления
- Кейсы: `TC-39`, `TC-40`
- Ожидалось: `400`.
- Фактически: `200`.

## BUG-07: Неверный статус для несуществующего `id` в `GET /api/1/item/{id}`
- Кейс: `TC-43`
- Ожидалось: `404`.
- Фактически: `400`.

## BUG-08: Неверный статус для пустого `id` в `GET /api/1/item/{id}`
- Кейс: `TC-44`
- Ожидалось: `400`.
- Фактически: `404`.

## BUG-09: Некорректная валидация `sellerID` в `GET /api/1/{sellerID}/item`
- Кейсы: `TC-51`, `TC-53`, `TC-54`
- Ожидалось: `400` для пустого/нулевого/отрицательного `sellerID`.
- Фактически:
  - `TC-51`: `405`
  - `TC-53`: `200` и пустой массив
  - `TC-54`: `200` и список объявлений для `sellerId=-1`

## BUG-10: Неверная обработка `id` в `GET /api/1/statistic/{id}`
- Кейсы: `TC-59`, `TC-60`
- Ожидалось:
  - `TC-59`: `404` для несуществующего `id`
  - `TC-60`: `400` для пустого `id`
- Фактически:
  - `TC-59`: `400`
  - `TC-60`: `404`
